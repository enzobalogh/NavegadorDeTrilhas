<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout da MainActivity — tela principal do aplicativo.
    Utiliza ScrollView + LinearLayout vertical para garantir
    compatibilidade com telas menores (API 17+).
-->
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_main"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp">

        <!-- ============================================================
             Ícone / Logo do aplicativo
             ============================================================ -->
        <ImageView
            android:id="@+id/iv_logo"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_marginBottom="8dp"
            android:src="@drawable/ic_trilha_logo"
            android:contentDescription="@string/desc_logo"
            android:scaleType="fitCenter" />

        <!-- ============================================================
             Título do aplicativo
             ============================================================ -->
        <TextView
            android:id="@+id/tv_titulo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="@color/colorPrimary"
            android:gravity="center"
            android:layout_marginBottom="8dp" />

        <!-- Subtítulo -->
        <TextView
            android:id="@+id/tv_subtitulo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/subtitulo_main"
            android:textSize="14sp"
            android:textColor="@color/text_secondary"
            android:gravity="center"
            android:layout_marginBottom="48dp" />

        <!-- ============================================================
             Botão: Registrar Trilha
             ============================================================ -->
        <Button
            android:id="@+id/btn_registrar_trilha"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="@string/btn_registrar_trilha"
            android:textSize="16sp"
            android:textAllCaps="false"
            android:backgroundTint="@color/colorPrimary"
            android:textColor="@color/white"
            android:drawableStart="@drawable/ic_add_location"
            android:drawablePadding="8dp"
            android:layout_marginBottom="16dp"
            android:elevation="4dp" />

        <!-- ============================================================
             Botão: Consultar Trilhas
             ============================================================ -->
        <Button
            android:id="@+id/btn_consultar_trilhas"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="@string/btn_consultar_trilhas"
            android:textSize="16sp"
            android:textAllCaps="false"
            android:backgroundTint="@color/colorSecondary"
            android:textColor="@color/white"
            android:drawableStart="@drawable/ic_list_trilhas"
            android:drawablePadding="8dp"
            android:layout_marginBottom="16dp"
            android:elevation="4dp" />

        <!-- ============================================================
             Botão: Configurações
             ============================================================ -->
        <Button
            android:id="@+id/btn_configuracoes"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="@string/btn_configuracoes"
            android:textSize="16sp"
            android:textAllCaps="false"
            android:backgroundTint="@color/colorAccent"
            android:textColor="@color/white"
            android:drawableStart="@drawable/ic_settings"
            android:drawablePadding="8dp"
            android:elevation="4dp" />

        <!-- Versão do app — rodapé -->
        <TextView
            android:id="@+id/tv_versao"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/versao_app"
            android:textSize="12sp"
            android:textColor="@color/text_secondary"
            android:gravity="center"
            android:layout_marginTop="48dp" />

    </LinearLayout>

</ScrollView>
